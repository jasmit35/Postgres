#
#  docker-compose.yaml
#
version: "3"
services:

  server:
    build: ../../server 
    container_name: postgres-server
    restart: always
    volumes:
      - postgres-data:/opt/app/postgres/data
    networks:
      - postgres-network
    ports:
      - 5432:5430

networks:
  postgres-network:
    external: true

volumes:
  postgres-data:
    external: true
